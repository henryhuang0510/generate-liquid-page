<div class="test-links">
  <h1>链接测试页面</h1>
  
  <div class="test-section">
    <h2>协议相对链接测试</h2>
    <p>以下链接应该能正常加载：</p>
    
    <!-- 协议相对链接 -->
    <script src="//cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="//cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    
    <div class="test-content">
      <p>如果 Tailwind CSS 加载成功，这个按钮应该有样式：</p>
      <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
        Tailwind 按钮
      </button>
      
      <p>如果 Font Awesome 加载成功，这里应该有图标：</p>
      <i class="fas fa-heart text-red-500 text-2xl"></i>
      <i class="fas fa-star text-yellow-500 text-2xl"></i>
    </div>
  </div>

  <div class="test-section">
    <h2>图片链接测试</h2>
    <p>协议相对图片链接：</p>
    <img src="//via.placeholder.com/300x200/007bff/ffffff?text=Test+Image" alt="测试图片">
  </div>

  <style>
    .test-links {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
      font-family: Arial, sans-serif;
    }
    
    .test-section {
      margin: 20px 0;
      padding: 20px;
      border: 1px solid #ddd;
      border-radius: 8px;
    }
    
    .test-content {
      margin: 10px 0;
    }
    
    button {
      margin: 10px 5px;
    }
    
    i {
      margin: 0 5px;
    }
  </style>

  <script>
    console.log('测试页面加载完成');
    console.log('当前页面协议:', window.location.protocol);
    console.log('当前页面 URL:', window.location.href);
    
    // 测试外部资源是否加载
    document.addEventListener('DOMContentLoaded', function() {
      console.log('DOM 加载完成');
      
      // 检查 Tailwind 是否加载
      if (window.tailwind) {
        console.log('✅ Tailwind CSS 加载成功');
      } else {
        console.log('❌ Tailwind CSS 加载失败');
      }
      
      // 检查 Font Awesome 是否加载
      const testIcon = document.querySelector('.fas');
      if (testIcon && getComputedStyle(testIcon, '::before').content !== 'none') {
        console.log('✅ Font Awesome 加载成功');
      } else {
        console.log('❌ Font Awesome 加载失败');
      }
    });
  </script>
</div> 